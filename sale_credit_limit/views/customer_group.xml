<odoo>
    <record model="ir.actions.act_window" id="action_customer_group">
        <field name="name">Customer Group</field>
        <field name="res_model">customer.group</field>
        <!-- IF ODOO Version <= 12.0 REQUIRE view_type (UNCOMMENT THIS) -->
        <!-- <field name="view_type">form</field> -->
        <field name='view_mode'>tree,form</field>
        <!-- <field name='view_id' ref='ref_view_id'/> -->
        <!-- <field name="view_ids" eval="[
                (5, 0, 0),
                (0, 0, {'view_mode': 'kanban', 'view_id': ref('module.kanban_ref_id'), 'sequence':1}),
                (0, 0, {'view_mode': 'tree', 'view_id': ref('module.tree_ref_id'), 'sequence':2}),
                (0, 0, {'view_mode': 'form', 'view_id': ref('module.form_ref_id'), 'sequence':3})
                ]"/> -->
    </record>

    <menuitem action="action_customer_group" id="customer_group_menu" name="Customer Group" sequence="11" parent="sale.menu_sale_config" />
</odoo>