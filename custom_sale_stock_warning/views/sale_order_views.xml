<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_inherit_warehouse_context" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.warehouse.context</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            
            <xpath expr="//field[@name='order_line']//field[@name='product_template_id']" position="attributes">
                <attribute name="context">{
                    'partner_id': parent.partner_id,
                    'quantity': product_uom_qty,
                    'pricelist': parent.pricelist_id,
                    'uom': product_uom,
                    'company_id': parent.company_id,
                    'default_list_price': price_unit,
                    'default_uom_id': product_uom,
                    'warehouse_id_filter': parent.warehouse_id
                }</attribute>
            </xpath>

        </field>
    </record>
</odoo>