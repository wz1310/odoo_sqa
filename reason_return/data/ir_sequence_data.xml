<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Sequences for picking.return.request -->
        <record id="seq_picking_return_request" model="ir.sequence">
            <field name="name">Picking Return Request</field>
            <field name="code">picking.return.request</field>
            <!-- <field name="prefix">"'RETURN/%(s)/%s/%s-' % (time.strftime('%Y'), time.strftime('%m'), time.strftime('%d'))"</field> -->
            <field name="prefix">RETURN/%(year)s/%(month)s/%(day)s-</field>
            <field name="padding">5</field>
            <field name="use_date_range">True</field>
        </record>

    </data>
</odoo>
